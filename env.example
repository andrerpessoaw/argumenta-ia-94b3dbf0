# Variáveis públicas (podem ficar no .env versionado — já estão em .env)
SUPABASE_PROJECT_ID="yitypxjfdcrematxznnr"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
SUPABASE_URL="https://yitypxjfdcrematxznnr.supabase.co"
VITE_SUPABASE_PROJECT_ID="yitypxjfdcrematxznnr"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
VITE_SUPABASE_URL="https://yitypxjfdcrematxznnr.supabase.co"

# ---------------------------------------------------------------------------
# SEGREDOS — NUNCA commitar. Configure em Netlify > Site settings > Environment
# variables. Sem eles o app builda, mas os recursos abaixo quebram em runtime.
# ---------------------------------------------------------------------------

# Usada pelo servidor para gerar tema (fallback local existe) e para CORRIGIR
# redações com IA (sem fallback — obrigatória para essa função funcionar).
# Pegue em https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# Chave de service role do Supabase (bypassa RLS). Usada nas rotas server-side
# administrativas (ex.: admin.tsx). Pegue em Supabase > Project Settings > API.
SUPABASE_SERVICE_ROLE_KEY=""
